language: python
dist: xenial
sudo: true

python:
    - 3.8
install:
    - pip install -r requirements-dev.txt codecov

script:
    - flake8
    - pytest projetomeu --cov=projecto
after_success:
    - codecov

